from setuptools import setup, find_packages

setup(
    name='DocxChain_¨PO',
    version='0.1',
    packages=find_packages(),
    scripts=['docxchain.py'],  # Replace 'main.py' with the name of your main file
    # other metadata...
)